TODO: checkout the old file

- error: invalid suffix `asdf` for number literal
  - help: the suffix must be one of the numeric types (`u32`, `isize`, `f32`, etc.)
  - okay, I can't use this error message for Sodigy