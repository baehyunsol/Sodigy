#[derive(Clone, Copy, Debug, Eq, Hash, PartialEq)]
pub enum Keyword {
    As,
    Assert,
    Else,
    Enum,
    Fn,
    Proc,
    If,
    Let,
    Match,
    Mod,
    Pub,
    Struct,
    Type,
    Use,
}
