#[derive(Clone, Copy, Debug, Eq, Hash, PartialEq)]
pub enum Keyword {
    As,
    Assert,
    Else,
    Enum,
    Fn,
    If,
    Impure,
    Let,
    Match,
    Mod,
    Pub,
    Struct,
    Type,
    Use,
}
