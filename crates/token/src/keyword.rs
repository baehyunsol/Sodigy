#[derive(Clone, Copy, Debug, Eq, Hash, PartialEq)]
pub enum Keyword {
    As,
    Assert,
    Else,
    Enum,
    Fn,
    Proc,
    Ndet,
    If,
    Let,
    Match,
    Mod,
    Pub,
    Struct,
    Type,
    Use,
}
