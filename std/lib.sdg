mod bool;
mod byte;
mod bytes;
mod char;
mod convert;
mod r#fn;
mod int;
mod list;
mod number;
mod op;
mod prelude;
mod random;
mod scalar;
mod string;
mod tuple;

use std.int.Int;
use std.string.String;

#[built_in]
#[lang_item("built_in.exit")]
impure fn exit() -> !;

// TODO: implement high-level `panic(msg: String) -> !;`
//       In order to do that, we need a syntax to execute actions.
#[built_in]
#[lang_item("built_in.panic")]
fn panic() -> !;

// TODO: implement high-level `print<T>(v: T) -> T;`
// TODO: return a ZST, when ZST optimization is implemented
#[built_in]
#[lang_item("built_in.print")]
impure fn print(v: String) -> Int;

// TODO: implement high-level `eprint<T>(v: T) -> T;`
// TODO: return a ZST, when ZST optimization is implemented
#[built_in]
#[lang_item("built_in.eprint")]
impure fn eprint(v: String) -> Int;

// TODO: It must be hidden to users.
// This is a magic function. It does nothing at runtime.
// It exists just to trick the type checker. It's used for
// trivial type conversions (e.g. `Char` -> `Scalar`).
#[built_in]
fn transmute<T, U>(v: T) -> U;
