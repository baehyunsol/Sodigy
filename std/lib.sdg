// Items in std can use a special decorator: `#[built_in]`.

mod bool;
mod byte;
mod bytes;
mod char;
mod int;
mod list;
mod number;
mod op;
mod prelude;
mod string;

use std.int.Int;

#[built_in]
fn panic() -> !;

#[built_in]
fn read_compound<T, U>(a: T, b: Int) -> U;
