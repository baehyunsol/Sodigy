# module fp;
# module math;
# module etc;

# temporary functions to see if the type checker works
def add_nums(a: Int, b: Int): Int = if a == b { a * 2 } else { add(a, b) };

def add(a: Int, b: Int): Int = if b == 0 { a } else { add(a, b - 1) };
