//# assert len(errors) == 1, f"expected exactly 1 error, got {len(errors)} errors"
//# assert errors[0].index == 45, f"expected error-45, got error-{errors[0].index}"

let x = match 0 {
    // To unambiguate this, you have to write `(0.)..`
    0... => 1,
    _ => 2,
};
