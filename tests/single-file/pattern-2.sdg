let (x, y) = (300, 400);

assert z == 301;
let z = x + 1;

struct Person = {
    name: String,
    age: Int,
};

let Person { name: my_name, .. } = Person { name: "Bae", age: 26 };

assert z == "Bae";
let z = my_name;
