//# assert len(errors) == 1, f"expected exactly 1 error, got {len(errors)} errors"
//# assert errors[0].index == 305, f"expected error-305, got {errors[0].index}"

#[note(\(x, x) => x + x)]
assert True;
