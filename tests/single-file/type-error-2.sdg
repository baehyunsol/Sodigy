//# assert len(errors) == 1, f"expected exactly 1 error, got {len(errors)} errors"
//# assert errors[0].index == 415, f"expected error-415 (type error), got error-{errors[0].index}"

let x = 3;
let y = "";
let z = [x, y];
