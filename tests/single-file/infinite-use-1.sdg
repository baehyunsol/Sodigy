//# assert len(errors) == 1, "there should be exactly 1 error"
//# assert len(warnings) == 0, "there should be no warnings"
//# assert all([error.index == 350 for error in errors]), f"expected error-350, but got {errors[0].index}"

use x as y;
use y as x;
