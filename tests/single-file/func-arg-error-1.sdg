//# assert len(errors) == 2, "there should be exactly 2 errors"
//# assert len(warnings) == 0, "there should be no warnings"
//# assert all([error.index == 340 for error in errors]), f"all errors are expected to be error-340, but some are not"
//# assert all(["`x`" in error.title for error in errors]), "all errors supposed to mention the keyword `x`, but some don't"

fn add(x, y, z) = x + y + z;

let double_x = add(10, 20, x=30);
let triple_x = add(10, x=0,x=0,x=0);
