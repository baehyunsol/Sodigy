//# assert len(errors) == 1, f"expected exactly 1 error, got {len(errors)} errors"
//# assert errors[0].index == 410, f"expected error-410, got error-{errors[0].index}"

fn zero() = 0;

#[impl(zero)]
fn add(a: Int, b: Int) -> Int = a + b;
