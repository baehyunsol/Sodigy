//% compile-error == 8

// This file is a sibling of `purity-1.sdg`
// All the `let` statements in this file must be a type error.
// Read `purity-1.sdg` for more context.

fn pure_func(n: Int) -> Int = n + 1;
ndet fn impure_func(n: Int) -> Int = std.random.random_int() + n;

let f3: NdetFn(Int) -> Int = pure_func;
let f5: Fn(Int) -> Int = impure_func;
let funcs4: [Fn(Int) -> Int] = [pure_func, impure_func];
let funcs6: [Fn(Int) -> Int] = [impure_func, impure_func];
let funcs7: [NdetFn(Int) -> Int] = [pure_func, impure_func];
let funcs8: [NdetFn(Int) -> Int] = [pure_func, pure_func];
let lambda3: NdetFn(Int) -> Int = \(n) => n + 1;
let lambda5: Fn(Int) -> Int = ndet \(n) => std.random.random_int() + n;
