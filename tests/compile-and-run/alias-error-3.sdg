//# assert len(errors) > 0, "there should be errors"
//# assert len(warnings) == 0, "there should be no warnings"
//# assert all([error.index == 315 for error in errors]), f"all errors are expected to be error-315, but some are not"

type x = y.a.b;
type y = x.a.b;
