//# assert len(errors) == 3, f"expected exactly 3 errors, got {len(errors)} errors"
//# assert all([error.index == 415 for error in errors]), "expected all the errors to be error-415, but some are not"

// This file is a sibling of `subtype-2.sdg`
// Read `subtype-2.sdg` for more context.

type Subtype = PureFn() -> ();
type Supertype = Fn() -> ();

let subtypes: [Subtype] = [];
let supertypes: [Supertype] = [];

let nested2: [[Subtype]] = [subtypes, supertypes];
let nested3: [[Subtype]] = [supertypes, subtypes];
let nested4: [[Subtype]] = [supertypes, supertypes];
