//% compile-error == 1

struct Foo<T> = {
    data: (T, Option<Foo<T>>),
};

fn new_foo<T>(x: T) = Foo { data: (x, None) };

#[associate(Foo<T>)]
fn concat<T>(self, x: T) -> Foo<T> = Foo { data: (x, Some(self)) };

let f = new_foo(300).concat(400).concat("");
