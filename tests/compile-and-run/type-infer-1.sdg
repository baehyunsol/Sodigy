fn map1<T>(ns: [T], f) = {
    let nx = f(ns[0]);

    if ns == [] { [] } else { [nx] ++ map1(ns[1..], f) }
};

#[assert_type(PureFn([Int]) -> Int)]
fn first(ns) = if ns == [] { 0 } else { ns[0] };

#[assert_type(PureFn(Int) -> Int)]
fn fibo(n) = if n < 2 { 1 } else { fibo(n - 1) + fibo(n - 2) };
