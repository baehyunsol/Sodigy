// fn f1(n: Int) -> Int = std.random.random_int() + n;  // purity error
fn f2(n: Int) -> Int = n + 1;
proc f3(n: Int) -> Int = std.random.random_int() + n;
proc f4(n: Int) -> Int = n + 1;  // purity warning

// let v1 = std.random.random_int();  // purity error

// let l1: Callable(Int) -> Int = \(n) => std.random.random_int() + n;  // purity error
// let l2: Fn(Int) -> Int = \(n) => std.random.random_int() + n;  // purity error
// let l3: Proc(Int) -> Int = \(n) => std.random.random_int() + n;  // type error, purity error

let l4: Callable(Int) -> Int = proc \(n) => std.random.random_int() + n;
// let l5: Fn(Int) -> Int = proc \(n) => std.random.random_int() + n;  // type error
let l6: Proc(Int) -> Int = proc \(n) => std.random.random_int() + n;

let l7: Callable(Int) -> Int = \(n) => n + 1;
let l8: Fn(Int) -> Int = \(n) => n + 1;
// let l9: Proc(Int) -> Int = \(n) => n + 1;  // type error

let l10: Callable(Int) -> Int = proc \(n) => n + 1;  // purity warning
// let l11: Fn(Int) -> Int = proc \(n) => n + 1;  // type error, purity warning
let l12: Proc(Int) -> Int = proc \(n) => n + 1;  // purity warning

// assert std.random.random_int() == 100;  // purity error
