struct Person = {
    name: String,
    age: Int,
    is_sodigian: Bool,
};

fn get_old(p: Person) -> Person = p `age (p.age + 1);

assert old_bae.age == 26;
let old_bae: Person = get_old(Person {
    name: "Bae",
    age: 25,
    is_sodigian: True,
});
