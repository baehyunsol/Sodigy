// Arguments of decorators are parsed in a complicated way, so we have to make sure that
// parse errors in the arguments are handled correctly.

//% compile-error == 2
//% compile-fail

#[note([][])]
assert True;

#[note([][])]
assert True;
