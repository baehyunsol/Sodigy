struct Person = {
    name: String,
    age: Int,
    is_rustacean: Bool,
};

func get_old(p: Person): Person = p `age (p.age + 1);

let old_bae: Person = get_old(Person {
    name: "Bae",
    age: 25,
    is_rustacean: True,
});

// TODO: assert_eq!(old_bae.age, 26)
